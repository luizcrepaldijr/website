---
import Image from "astro/components/Image.astro";
import Accordion from "./Accordion.astro";
import ButtonArrowSquare from "./buttons/ButtonArrowSquare.astro";

import TransformacaoDigitalImg from "../images/solucoes/transformacao-digital-solucao.webp";
import Industria40Img from "../images/solucoes/industria-4-0-solucao.webp";
import EngenhariaImg from "../images/solucoes/engenharia-solucao.webp";
import Agricultura40Img from "../images/solucoes/agricultura-4-0-solucao.webp";
import IotImg from "../images/solucoes/iot-solucao.webp";
import RoboticaImg from "../images/home/servicos/robotica.webp";
import RealidadeDigitalImg from "../images/home/servicos/realidade-digital.webp";

const solucoesData = [
    {
        id: "transformacion-digital",
        title: "Transformación digital",
        subtitle:
            'Capacitamos su negocio a través de soluciones inteligentes, <span class="br"></span> conectadas y orientadas por datos.',
        description:
            "Ayudamos a las organizaciones a desbloquear todo el potencial de sus datos y tecnologías integrando herramientas inteligentes, automatizando procesos y permitiendo insights en tiempo real que impulsan la toma de decisiones estratégicas.",
        video: "/videos/home/transformacao-digital.webm",
        image: TransformacaoDigitalImg,
        accordions: [
            {
                title: "Consultoría",
                description:
                    "Ayudamos a empresas de bienes de consumo a impulsar su transformación digital con enfoque en un impacto real. Con experiencia práctica en operaciones industriales, definimos proyectos, tecnologías y estrategias que generan resultados concretos, mediante planes de trabajo escalables y alineados al negocio.",
            },
            {
                title: "Integración de inteligencia artificial",
                description:
                    "Desarrollamos soluciones basadas en IA diseñadas para resolver desafíos operativos específicos. Al combinar nuestra experiencia en la industria con capacidades avanzadas de IA, creamos soluciones que aumentan la eficiencia, elevan la productividad y generan ganancias medibles.",
            },
            {
                title: "Data Analytics & Applied Data Science",
                description:
                    "Estructuramos pipelines, modelos analíticos y capas de visualización que transforman datos brutos en insights accionables para la operación y toma de decisiones.",
            },
            {
                title: "Entrenamiento y Bootcamp",
                description:
                    "Capacitación técnica e estratégica para equipos, garantizando autonomía, adopción de las soluciones implementadas y sostenibilidad de la transformación digital.",
            },
        ],
    },
    {
        id: "industria-40",
        title: "Industria 4.0",
        subtitle:
            'Conectamos el mundo físico y digital para aumentar la <span class="br"></span> visibilidad, el control y el rendimiento industrial.',
        description:
            "Actuamos en entornos industriales donde la falta de integración entre máquinas, sistemas y datos limita la eficiencia operativa. Implementamos soluciones de Industria 4.0 que conectan la planta de producción al entorno digital, permitiendo operaciones más predecibles, seguras y basadas en datos.",
        video: "/videos/home/industria-4-0.webm",
        image: Industria40Img,
        accordions: [
            {
                title: "Infraestructura de datos y redes OT",
                description:
                    "Diseño e implementación de arquitecturas robustas para la recolección e integración de datos industriales, conectando sistemas de operación y gestión de forma segura.",
            },
            {
                title: "Ciberseguridad",
                description:
                    "Protección de redes, datos y activos críticos, reduciendo riesgos operativos y garantizando la continuidad de la producción en entornos industriales.",
            },
            {
                title: "Programación",
                description:
                    "Desarrollo y ajustes de programación necesarios para la integración y el correcto funcionamiento de los sistemas industriales.",
            },
            {
                title: "Soporte en terreno y remoto",
                description:
                    "Soporte técnico continuo para garantizar la estabilidad operativa, respuesta rápida a fallas y mantenimiento de los sistemas implementados.",
            },
        ],
    },
    {
        id: "ingenieria",
        title: "Ingeniería",
        subtitle:
            'Transformamos ideas complejas en soluciones técnicas <span class="br"></span> viables y eficientes.',
        description:
            "Ofrecemos servicios de ingeniería de punta a punta para proyectos que exigen rigor técnico, planificación y ejecución precisa. Actuamos desde la concepción hasta la implementación, conectando ingeniería, tecnología y operación para reducir riesgos y optimizar resultados.",
        video: "/videos/home/engenharia.webm",
        image: EngenhariaImg,
        accordions: [
            {
                title: "Gestión de proyectos",
                description:
                    "Gestión técnica con enfoque en alcance, tiempo, costo y calidad, garantizando la alineación entre equipos y ejecución eficiente.",
            },
            {
                title: "Estudios conceptuales y de viabilidad",
                description:
                    "Análisis técnicos iniciales para orientar decisiones estratégicas y evaluar la viabilidad de proyectos de forma objetiva y aplicada.",
            },
            {
                title: "Estimaciones de capital y documentación técnica",
                description:
                    "Elaboración de estimaciones de costos y documentación necesaria para fundamentar decisiones de inversión y ejecución.",
            },
            {
                title: "Modelado y simulación",
                description:
                    "Modelado y simulaciones avanzadas para probar escenarios, anticipar riesgos y optimizar soluciones antes de la implementación física.",
            },
            {
                title: "Proyectos eléctricos, mecánicos y de automatización",
                description:
                    "Proyectos completos e integrados, alineados a las necesidades de la operación y a las tecnologías involucradas.",
            },
        ],
    },
    {
        id: "agricultura-40",
        title: "Agricultura 4.0",
        subtitle:
            'Ayudamos a productores y operaciones agrícolas a tomar mejores decisiones usando datos reales del campo.',
        description:
            "Aplicamos tecnología para transformar información del suelo y el clima en inteligencia práctica, permitiendo un mejor riego, reducción de desperdicios, aumento de la productividad y una operación más sostenible y predecible.",
        video: "/videos/home/agricultura-4-0.webm",
        image: Agricultura40Img,
        accordions: [
            {
                title: "Monitoreo inteligente del suelo",
                description:
                    "Recolección continua de datos como humedad a diferentes profundidades, temperatura y conductividad eléctrica, ofreciendo una visión precisa de las condiciones reales del cultivo.",
            },
            {
                title: "Eficiencia hídrica guiada por datos",
                description:
                    "Soporte a las decisiones de riego en el momento exacto y en la cantidad necesaria, reduciendo el desperdicio y generando ahorros significativos en el uso del agua.",
            },
            {
                title: "Plataforma integrada de datos agrícolas",
                description:
                    "Visualización en tiempo real, historial de mediciones, alertas y seguimiento del desempeño hidráulico y productivo de la operación.",
            },
            {
                title: "Conversión de datos técnicos en impacto financiero",
                description:
                    "Análisis del uso del agua con conversión en indicadores financieros, facilitando el control de costos y la planificación de la operación.",
            },
            {
                title: "Conectividad em áreas remotas",
                description:
                    "Integración con soluciones de conectividad satelital, garantizando el monitoreo continuo incluso en regiones sin infraestructura local.",
            },
        ],
    },
    {
        id: "iot",
        title: "IoT",
        subtitle:
            'Conectamos activos, procesos y entornos para generar inteligencia operativa, previsibilidad y eficiencia en <span class="br"></span> tiempo real.',
        description:
            "Ayudamos a las empresas a ver en tiempo real lo que ocurre en la operación, aplicando IoT para transformar señales físicas en inteligencia operativa práctica.<br /><br />Hacemos posible anticipar fallas, optimizar recursos, reducir desperdicios y tomar decisiones más rápidas, seguras y basadas en datos reales.",
        video: "/videos/iot/iot-hero.webm",
        image: IotImg,
        buttons: [
            { text: "IoT para la industria", link: "/es/soluciones/iot?panel=ind" },
            { text: "IoT para la agricultura", link: "/es/soluciones/iot?panel=agri" },
        ],
    },
    {
        id: "robotics",
        title: "Robótica",
        subtitle:
            'Elevamos la automatización industrial conectando robots,<span class="br"></span> datos y procesos productivos.',
        description:
            "Desarrollamos e integramos sistemas robóticos en entornos industriales para aumentar la productividad, seguridad y eficiencia operativa. La robótica actúa como una capa de inteligencia en la operación, conectando máquinas, personas y datos para generar previsibilidad y evolución continua de los procesos.",
        video: "/videos/home/robotica.webm",
        image: RoboticaImg,
        accordions: [
            {
                title: "Robots humanoides",
                description:
                    "Lorem ipsum dolor sit amet, consectetur adipiscing elit.",
            },
            {
                title: "Robots cuadrúpedos",
                description:
                    "Lorem ipsum dolor sit amet, consectetur adipiscing elit.",
            },
            {
                title: "Robots colaborativos (cobots)",
                description:
                    "Lorem ipsum dolor sit amet, consectetur adipiscing elit.",
            },
            {
                title: "Integración con sistemas industriales",
                description:
                    "Lorem ipsum dolor sit amet, consectetur adipiscing elit.",
            },
        ],
        buttons: [
            {
                text: "Saber más sobre este servicio",
                link: "/es/soluciones/robotica",
            },
        ],
    },
    {
        id: "digital-reality",
        title: "Realidad Digital",
        subtitle:
            'Entrenamiento y ejecución operativa con entornos <span class="br"></span> inmersivos integrados a la industria.',
        description:
            "Aplicamos tecnologías de realidad virtual, aumentada y mixta para crear simulaciones y orientaciones digitales directamente en el contexto de la operación industrial. Esto permite que los equipos aprendan, practiquen y ejecutun tareas con más seguridad, eficiencia y estandarización.",
        video: "/videos/home/realidade-digital.webm",
        image: RealidadeDigitalImg,
        accordions: [
            {
                title: "Entrenamiento en realidad virtual",
                description:
                    "Lorem ipsum dolor sit amet, consectetur adipiscing elit.",
            },
            {
                title: "Orientación en realidad aumentada",
                description:
                    "Lorem ipsum dolor sit amet, consectetur adipiscing elit.",
            },
            {
                title: "Realidad mixta aplicada a la operación",
                description:
                    "Lorem ipsum dolor sit amet, consectetur adipiscing elit.",
            },
            {
                title: "Mantenimiento guiado por instrucciones visuales",
                description:
                    "Lorem ipsum dolor sit amet, consectetur adipiscing elit.",
            },
        ],
    },
];
---

<div class="solucoes-section-wrapper">
    <div class="solucoes-desktop-container">
        <div class="solucoes-desktop-pinned">
            <div class="solucoes-bg-container">
                {
                    solucoesData.map((solucao, index) => (
                        <div class="solucoes-bg-layer" data-index={index}>
                            {solucao.video ? (
                                <video
                                    src={solucao.video}
                                    class="solucoes-bg-video"
                                    autoplay
                                    muted
                                    loop
                                    playsinline
                                />
                            ) : (
                                <Image
                                    src={solucao.image}
                                    alt={solucao.title}
                                    class="solucoes-bg-img"
                                />
                            )}
                            <div class="solucoes-bg-overlay" />
                        </div>
                    ))
                }
            </div>

            <div class="container-boxed solucoes-desktop-content">
                <div class="solucoes-menu">
                    <ul class="solucoes-nav-list">
                        {
                            solucoesData.map((solucao, index) => (
                                <li
                                    class="solucoes-nav-item"
                                    data-index={index}
                                >
                                    <div class="nav-indicator" />
                                    <span>{solucao.title}</span>
                                </li>
                            ))
                        }
                    </ul>
                </div>

                <div class="solucoes-panels">
                    {
                        solucoesData.map((solucao, index) => (
                            <div class="solucoes-panel" data-index={index}>
                                <h2 class="panel-title">{solucao.title}</h2>
                                <h3
                                    class="panel-subtitle"
                                    set:html={solucao.subtitle}
                                />
                                <p
                                    class="panel-desc"
                                    set:html={solucao.description}
                                />

                                {solucao.accordions && (
                                    solucao.id === "robotics" || solucao.id === "digital-reality" ? (
                                        <ul class="solucoes-list-items">
                                            {solucao.accordions.map((acc) => (
                                                <li class="solucoes-list-item">
                                                    <span class="arrow">→</span>
                                                    <span class="text">{acc.title}</span>
                                                </li>
                                            ))}
                                        </ul>
                                    ) : (
                                        <div class="accordion-wrapper">
                                            {solucao.accordions.map((acc) => (
                                                <Accordion
                                                    title={acc.title}
                                                    description={acc.description}
                                                />
                                            ))}
                                        </div>
                                    )
                                )}

                                {solucao.buttons && (
                                    <div class="buttons-wrapper">
                                        {solucao.buttons.map((btn) => (
                                            <ButtonArrowSquare
                                                text={btn.text}
                                                link={btn.link}
                                            />
                                         ))}
                                     </div>
                                )}
                            </div>
                        ))
                    }
                </div>

                <div class="solucoes-loader">
                    <svg
                        class="progress-ring"
                        width="45"
                        height="45"
                        viewBox="0 0 45 45"
                    >
                        <circle
                            class="progress-ring__circle_bg"
                            stroke="rgba(255,255,255,0.2)"
                            stroke-width="3"
                            fill="transparent"
                            r="21"
                            cx="22.5"
                            cy="22.5"></circle>
                        <circle
                            class="progress-ring__circle"
                            stroke="#FCFCFD"
                            stroke-width="3"
                            fill="transparent"
                            r="21"
                            cx="22.5"
                            cy="22.5"></circle>
                    </svg>
                </div>
            </div>
        </div>
    </div>

    <div class="solucoes-mobile container-boxed">
        {
            solucoesData.map((solucao) => (
                <div class="solucoes-item">
                    {solucao.video ? (
                        <video
                            src={solucao.video}
                            class="solucoes-item-video"
                            autoplay
                            muted
                            loop
                            playsinline
                        />
                    ) : (
                        <Image
                            src={solucao.image}
                            alt={solucao.title}
                            class="solucoes-item-img"
                        />
                    )}
                    <div class="content">
                        <h2>{solucao.title}</h2>
                        <h3 set:html={solucao.subtitle} />
                        <p set:html={solucao.description} />

                        {solucao.accordions && (
                            solucao.id === "robotics" || solucao.id === "digital-reality" ? (
                                <ul class="solucoes-list-items">
                                    {solucao.accordions.map((acc) => (
                                        <li class="solucoes-list-item">
                                            <span class="arrow">→</span>
                                            <span class="text">{acc.title}</span>
                                        </li>
                                    ))}
                                </ul>
                            ) : (
                                <div class="accordion-wrapper">
                                    {solucao.accordions.map((acc) => (
                                        <Accordion
                                            title={acc.title}
                                            description={acc.description}
                                        />
                                    ))}
                                </div>
                            )
                        )}

                        {solucao.buttons && (
                            <div class="accordion-wrapper">
                                {solucao.buttons.map((btn) => (
                                    <ButtonArrowSquare
                                        text={btn.text}
                                        link={btn.link}
                                    />
                                ))}
                            </div>
                        )}
                    </div>
                </div>
            ))
        }
    </div>
</div>

<style>
    .solucoes-desktop-container {
        display: none; /* Escondido no mobile */
    }

    .solucoes-mobile {
        display: flex; /* Escondido apenas via media query no desktop */
        flex-direction: column;
        gap: 80px;
        padding: 60px 20px;
    }

    .solucoes-item-img {
        width: 100%;
        height: auto;
        border-radius: 8px;
        margin-bottom: 24px;
        object-fit: cover;
    }

    .solucoes-item-video {
        width: 100%;
        max-height: 250px;
        border-radius: 8px;
        margin-bottom: 24px;
        object-fit: cover;
        mask-image:
            linear-gradient(
                to right,
                transparent 0%,
                black 15%,
                black 85%,
                transparent 100%
            ),
            linear-gradient(
                to bottom,
                transparent 0%,
                black 15%,
                black 85%,
                transparent 100%
            );
        mask-composite: intersect;
        -webkit-mask-image:
            linear-gradient(
                to right,
                transparent 0%,
                black 15%,
                black 85%,
                transparent 100%
            ),
            linear-gradient(
                to bottom,
                transparent 0%,
                black 15%,
                black 85%,
                transparent 100%
            );
        -webkit-mask-composite: source-in;
    }

    .solucoes-item .content {
        display: flex;
        flex-direction: column;
        gap: 16px;
    }

    .solucoes-item h2 {
        font-size: 32px;
        color: var(--neutral-white);
        font-weight: 600;
        margin: 0;
    }

    .solucoes-item h3 {
        font-size: 20px;
        font-weight: 400;
        color: var(--neutral-light-gray);
        margin: 0;
    }

    .solucoes-item h3 :global(.br) {
        display: none;
    }

    .solucoes-item p {
        font-size: 16px;
        color: var(--neutral-light-gray);
        opacity: 0.8;
    }

    .accordion-wrapper {
        display: flex;
        flex-direction: column;
        gap: 0px;
        margin-top: 20px;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
    }

    .accordion-wrapper accordion-item {
        transition: all 0.4s ease;
    }

    /* Efeito de escurecer os outros quando um está aberto */
    .accordion-wrapper:has(.active) :global(accordion-item:not(.active)) {
        opacity: 0.6 !important;
    }

    .solucoes-list-items {
        display: flex;
        flex-direction: column;
        gap: 0px;
        margin-top: 20px;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        list-style: none;
        padding: 0;
    }

    .solucoes-list-item {
        display: flex;
        align-items: center;
        gap: 16px;
        padding: 24px 0;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        color: white;
        font-size: clamp(16px, 1.2vw + 4px, 20px);
        font-weight: 300;
        line-height: 1.4;
    }

    @media (max-width: 1440px) {
        .solucoes-list-item {
            font-size: 17px;
        }
    }

    .solucoes-list-item .arrow {
        font-weight: 300;
    }

    @media (min-width: 801px) {
        .solucoes-mobile {
            display: none !important;
        }

        .solucoes-desktop-container {
            display: block;
            position: relative;
            background-color: var(--neutral-background-dark, #0b0c10);
        }

        .solucoes-desktop-pinned {
            position: relative;
            height: 100vh;
            width: 100%;
            overflow: hidden;
            display: flex;
            align-items: center;
        }

        .solucoes-bg-container {
            position: absolute;
            top: 0;
            left: 10vw;
            width: 30vw; /* Slightly less than half to leave breathing room for text if needed */
            height: 100%;
            z-index: 0;
        }

        .solucoes-bg-layer {
            position: absolute;
            top: 0;
            left: 20%;
            width: 80%;
            height: 100%;
            opacity: 0;
            transition: opacity 0.5s ease;
        }

        .solucoes-bg-layer.active {
            opacity: 1;
            z-index: 1;
        }

        .solucoes-bg-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .solucoes-bg-video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .solucoes-bg-overlay {
            position: absolute;
            inset: 0;
            background:
                linear-gradient(
                    to right,
                    var(--neutral-background-dark, #0b0c10) 0%,
                    transparent 15%,
                    transparent 70%,
                    var(--neutral-background-dark, #0b0c10) 100%
                ),
                linear-gradient(
                    to bottom,
                    var(--neutral-background-dark, #0b0c10) 0%,
                    transparent 15%,
                    transparent 70%,
                    var(--neutral-background-dark, #0b0c10) 100%
                );
        }

        .solucoes-desktop-content {
            position: absolute; /* Mudado para absolute para conseguirmos centralizar com limites exatos */
            left: 50%;
            transform: translateX(-50%);
            z-index: 2;
            display: flex;
            justify-content: space-between;
            width: clamp(
                1100px,
                90%,
                1620px
            ); /* Exactly matching global container-boxed width */
            padding-inline: clamp(
                15px,
                4vw,
                40px
            ); /* Plus standard padding 30px essentially */
            height: 100vh;
        }

        .solucoes-menu {
            width: 35%;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding-bottom: 0;
            padding-top: 20vh;
            padding-left: 4%; /* Movendo para o centro, mas ainda à esquerda */
        }

        .solucoes-nav-list {
            list-style: none;
            padding: 0;
            margin: 0;
            display: flex;
            flex-direction: column;
            gap: 16px;
        }

        .solucoes-nav-item {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 18px;
            color: rgba(255, 255, 255, 0.4);
            transition: color 0.3s ease, font-size 0.3s ease;
            position: relative;
            cursor: pointer;
        }

        .nav-indicator {
            width: 0;
            height: 2px;
            background-color: var(--primary-blue, #0056b3);
            transition: width 0.3s ease;
        }

        .solucoes-nav-item.active,
        .solucoes-nav-item:hover {
            color: var(--neutral-white);
            font-weight: 500;
            font-size: 20px;
        }

        .solucoes-nav-item.active .nav-indicator,
        .solucoes-nav-item:hover .nav-indicator {
            width: 30px;
        }

        .solucoes-panels {
            width: 60%;
            position: relative;
            height: 100vh; /* Changed from 100% to 100vh */
            display: flex;
            align-items: center;
            max-width: 600px; /* Constrain max width of the right panel text content to avoid blowing up on ultrawides */
            margin-right: 0px;
            padding-left: 5%;
        }

        .solucoes-panel {
            position: absolute;
            top: 50%;
            left: 0;
            width: 100%;
            transform: translateY(-40%);
            opacity: 0;
            pointer-events: none;
            transition:
                opacity 0.5s ease,
                transform 0.5s ease;
            display: flex;
            flex-direction: column;
            gap: 16px;
        }

        .solucoes-panel.active {
            opacity: 1;
            transform: translateY(-50%);
            pointer-events: auto;
            z-index: 2;
        }

        .panel-title {
            font-size: 40px;
            color: var(--neutral-white);
            font-weight: 300;
            margin: 0;
            margin-bottom: 8px;
        }

        .panel-subtitle {
            font-size: 20px;
            font-weight: 300;
            color: var(--neutral-light-gray);
            margin: 0;
        }

        .panel-subtitle :global(.br) {
            display: block;
            content: "";
        }

        .panel-desc {
            font-size: 16px;
            color: var(--neutral-light-gray);
            opacity: 0.8;
            line-height: 1.6;
            margin-bottom: 24px;
        }

        .buttons-wrapper {
            display: flex;
            flex-direction: column;
            gap: 16px;
        }

        .solucoes-loader {
            position: absolute;
            bottom: 8vh;
            right: 0px;
            z-index: 10;
        }

        .progress-ring {
            transform: rotate(-90deg);
        }

        .progress-ring__circle {
            transition: stroke-dashoffset 0.1s ease;
        }
    }
</style>

<script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";

    gsap.registerPlugin(ScrollTrigger);

    function initSolucoesGSAP() {
        const desktopContainer = document.querySelector(
            ".solucoes-desktop-container",
        );
        if (!desktopContainer || window.innerWidth <= 800) return;

        const pinnedSection = document.querySelector(
            ".solucoes-desktop-pinned",
        );
        const bgLayers = gsap.utils.toArray(".solucoes-bg-layer");
        const navItems = gsap.utils.toArray(".solucoes-nav-item");
        const panels = gsap.utils.toArray(".solucoes-panel");
        const circle = document.querySelector(".progress-ring__circle");

        const totalItems = navItems.length;

        // Configuração do círculo do SVG para o Loader
        let circumference = 0;
        if (circle) {
            const radius = (circle as SVGCircleElement).r.baseVal.value;
            circumference = radius * 2 * Math.PI;
            (circle as SVGCircleElement).style.strokeDasharray =
                `${circumference} ${circumference}`;
            (circle as SVGCircleElement).style.strokeDashoffset =
                `${circumference}`;
        }

        // Primeiro item ativo logo de inicio
        if (bgLayers[0]) (bgLayers[0] as HTMLElement).classList.add("active");
        if (navItems[0]) (navItems[0] as HTMLElement).classList.add("active");
        if (panels[0]) (panels[0] as HTMLElement).classList.add("active");

        // Cria a distância de scroll baseado no total de itens (ex: 70vh cada para demorar bastante o scroll)
        const scrollDistance = totalItems * 80;

        const trigger = ScrollTrigger.create({
            trigger: desktopContainer,
            pin: pinnedSection,
            start: "top top",
            end: `+=${scrollDistance}%`,
            scrub: true,
            onUpdate: (self) => {
                const progress = self.progress; // Retorna valor entre 0 e 1

                // Atualizar o path do Loader
                if (circle) {
                    const offset = circumference - progress * circumference;
                    (circle as SVGCircleElement).style.strokeDashoffset =
                        `${offset}`;
                }

                // Identificar o índice atual baseado no progresso
                let activeIndex = Math.floor(progress * totalItems);
                if (activeIndex >= totalItems) activeIndex = totalItems - 1;

                updateActiveItem(activeIndex);
            },
        });

        navItems.forEach((item, index) => {
            (item as HTMLElement).addEventListener("click", () => {
                const start = trigger.start;
                const end = trigger.end;
                const progress = (index + 0.1) / totalItems;
                const targetScroll = start + progress * (end - start);

                window.scrollTo({
                    top: targetScroll,
                    behavior: "smooth",
                });
            });
        });

        function updateActiveItem(index: number) {
            bgLayers.forEach((el, i) => {
                if (i === index) (el as HTMLElement).classList.add("active");
                else (el as HTMLElement).classList.remove("active");
            });

            navItems.forEach((el, i) => {
                if (i === index) (el as HTMLElement).classList.add("active");
                else (el as HTMLElement).classList.remove("active");
            });

            panels.forEach((el, i) => {
                if (i === index) (el as HTMLElement).classList.add("active");
                else (el as HTMLElement).classList.remove("active");
            });
        }
    }

    document.addEventListener("DOMContentLoaded", initSolucoesGSAP);

    // Tratamento Astro View Transitions
    document.addEventListener("astro:page-load", () => {
        // Garantindo funcionamento se o usuário navegar e regressar pra página
        ScrollTrigger.getAll().forEach((t) => {
            // Em caso de múltiplos ScrollTriggers (Workflow e afins) matar o que tem esse trigger específico evitaria problemas, mas podemos deixar global para testes rápidos se for tranquilo.
            if (t.trigger?.classList.contains("solucoes-desktop-container")) {
                t.kill();
            }
        });
        initSolucoesGSAP();
    });
</script>
